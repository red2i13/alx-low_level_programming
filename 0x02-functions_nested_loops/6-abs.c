#include "main.h"

/**
 * _abs - check the code
 *
 * Description: Function that computes the absolute value of an integer.
 * @n: integer to calulate absolute value
 * Return: The absolute value of integer X
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}

	else
	{
		return (n);
	}
}
